import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    message: {
        fontSize: 24,
        textAlign: 'center',
        fontWeight: '200',
        marginVertical: 15,
    },
    sub:{
        fontSize: 16,
        fontWeight: '200',
    }
});